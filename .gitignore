# ============================================================
# 这是一个 12G 的数据湖，真实代码只有约 600K。
# 用【白名单】而不是黑名单 —— 黑名单在这种比例下必然漏（实测第一版就漏了
# 6.2G 的 DuckDB 溢写临时文件、60M 的二进制、一堆 zip）。
#
# 原则：只提交【能重建数据的代码】。
#   数据可由 build/*.py 从 raw 重建；raw 可由 raw/**/_ingest/*.py 重抽。
# ============================================================

# 1) 默认忽略一切
*

# 2) 但要能进入子目录（否则下面的放行规则匹配不到深层文件）
!*/

# 3) 放行代码与文档
!*.py
!*.md
!*.sh
!*.sql
!.gitignore

# 4) 放行少量小型元数据（77 张 JQ 表的清单，抽取脚本的产出）
!raw/jq/_ingest/jq_tables.csv

# 5) 最后再排除「虽然是 .py 但不该入库」的 —— 顺序重要，后匹配者胜
__pycache__/
*.py[cod]
raw/hf/_ingest/venv/
.tmp/
