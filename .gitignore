# ============================================================
# 这是一个 12G 的数据湖，真实代码只有约 600K。
# 用【白名单】而不是黑名单 —— 黑名单在这种比例下必然漏（实测第一版就漏了
# 6.2G 的 DuckDB 溢写临时文件、60M 的二进制、一堆 zip）。
#
# 原则：只提交【能重建数据的代码】。
#   数据可由 build/*.py 从 raw 重建；raw 可由 raw/**/_ingest/*.py 重抽。
# ============================================================

# 1) 默认忽略一切
*

# 2) 但要能进入子目录（否则下面的放行规则匹配不到深层文件）
!*/

# 3) 放行代码与文档
!*.py
!*.md
!*.sh
!*.sql
!.gitignore

# 4) 放行少量小型元数据（77 张 JQ 表的清单，抽取脚本的产出）
!raw/jq/_ingest/jq_tables.csv

# 4b) 调度配置 = 代码。launchd 的 plist 决定每日同步几点跑，
#     丢了就等于同步默默停了（而"停了"这件事本身不报错）。
!_manifest/*.plist

# 4c) 对数用的真值快照。**不是数据管道的一部分**，是证据 ——
#     上一次没有它，我们用"自己按文档重建的 beta"做单变量隔离，
#     得出了一个统计显著但完全错误的结论（见 docs/evidence/README.md）。
#     刻意保持很小：只在需要定案某个外部口径时加一份。
!docs/evidence/*.csv

# 5) 最后再排除「虽然是 .py 但不该入库」的 —— 顺序重要，后匹配者胜
__pycache__/
*.py[cod]
raw/hf/_ingest/venv/
.tmp/
